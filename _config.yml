name: Your New Jekyll Site
markdown: kramdown
highlighter: rouge
exclude: [bin, vendor]
permalink: /bliki/:title.html
timezone: Europe/Oslo

excerpt_separator: "\n\n\n"

collections:
  some:
    output: true

defaults:
  - scope:
      path: ""
      type: some
    values:
      layout: default
